development:
  sessions:
    default:
      hosts:
        - localhost
      database: vimage_development
      options:
        timeout: 3
        max_retries: 5

production:
  sessions:
    default:
      hosts:
        - localhost
      database: vimage_production
      options:
        timeout: 3
        max_retries: 5
